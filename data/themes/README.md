# themes/

Per-tenant white-label themes live here, one folder per tenant
(`<tenant>/config.css` + optional `logo.*`, `favicon.*`, `theme.json`).
Mounted into the container at `/app/data/themes`.

See [docs/theming.md](../../docs/theming.md) and the ready-to-copy
[examples/themes/](../../examples/themes/).
